# Один .env в каталоге проекта (те же API_URL, SERIAL_PORT, что и для compose).
services:
  scanner:
    build: .
    container_name: scanner-old
    restart: unless-stopped
    env_file:
      - .env
    devices:
      - "${SERIAL_PORT}:${SERIAL_PORT}"